binaries:
    bazel_nojdk:
        title: "Bazel NoJDK"
        url_template: "https://github.com/bazelbuild/bazel/releases/download/{version}/bazel_nojdk-{clean_version}-windows-x86_64.exe"
        filename_template: "bazel_nojdk-{clean_version}-windows-x86_64.exe"
        compressed_name_template: "bazel_nojdk-{clean_version}-windows-x86_64.zip"

    bazelisk:
        title: "Bazelisk"
        url_template: "https://github.com/bazelbuild/bazelisk/releases/download/{version}/bazelisk-windows-amd64.exe"
        filename_template: "bazelisk-windows-amd64.exe"
        compressed_name_template: "bazelisk-{clean_version}-windows-amd64.zip"

    buildifier:
        title: "Buildifier"
        url_template: "https://github.com/bazelbuild/buildtools/releases/download/{version}/buildifier-windows-amd64.exe"
        filename_template: "buildifier-windows-amd64.exe"
        compressed_name_template: "buildifier-{clean_version}-windows-amd64.zip"

    buildozer:
        title: "Buildozer"
        url_template: "https://github.com/bazelbuild/buildtools/releases/download/{version}/buildozer-windows-amd64.exe"
        filename_template: "buildozer-windows-amd64.exe"
        compressed_name_template: "buildozer-{clean_version}-windows-amd64.zip"

    copy_directory:
        title: "bazel-lib: Copy Directory"
        url_template: https://github.com/bazel-contrib/bazel-lib/releases/download/{version}/copy_directory-windows_amd64.exe
        filename_template: copy_directory-windows_amd64.exe
        compressed_name_template: copy_directory-{clean_version}-windows-x86_64.zip
